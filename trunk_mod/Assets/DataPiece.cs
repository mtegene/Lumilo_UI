using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[Serializable]
public class DataPiece
{

    [SerializeField]
    public string id; //this will be the key, the user name for teachers and students

    //might wanna do a toString function that returns the string represenation of everything so only that is serialized???

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
